var PI = 3;

class Circle {
  init(radius) {
    this.radius = radius;
  }

  get area() {
    return PI * this.radius * this.radius;
  }
}

var c = Circle(2);
c.area = 2; // error: property 'area' of 'Circle' object is read-only
