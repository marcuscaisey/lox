class Foo {
  get bar() {}
  static bar() {}

  // error: 'bar' has already been declared as a property accessor
  // lint error: 'bar' has already been declared as a property accessor
  bar() {}
}

_ = Foo;
