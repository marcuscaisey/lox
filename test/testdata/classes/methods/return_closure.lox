class AdderFactory {
    init(n) {
        this._n = n;
    }

    newAdder() {
        fun adder(x) {
            return x + this._n;
        }
        return adder;
    }
}

var adderFactory = AdderFactory(3);
var add3 = adderFactory.newAdder();
print add3(4); // prints: 7
